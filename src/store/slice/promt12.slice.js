import { createSlice } from "@reduxjs/toolkit";

export const promt12Slice = createSlice({
	name: "promt12",
	initialState: "",
	reducers: {},
});

export const {} = promt12Slice.actions;

export default promt12Slice.reducer;
