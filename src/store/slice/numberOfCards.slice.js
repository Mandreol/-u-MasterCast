import { createSlice } from "@reduxjs/toolkit";

export const numberOfCardsSlice = createSlice({
	name: "numberOfCards",
	initialState: 0,
	reducers: {},
});

export const {} = numberOfCardsSlice.actions;

export default numberOfCardsSlice.reducer;
