import { createSlice } from "@reduxjs/toolkit";

export const sortingTypeSlice = createSlice({
	name: "sortingType",
	initialState: "",
	reducers: {},
});

export const {} = sortingTypeSlice.actions;

export default sortingTypeSlice.reducer;
